# GP Access Simulation

## Quick start
```bash
pip install -r requirements.txt
python main.py run params.yaml
```

Outputs are written under outputs/run_seed_* with manifest.json, events.csv, overall.csv, gaps.csv, and risk_deciles.csv.

